# Data Model: 各ファイルの削減詳細

**Phase**: 1 | **Date**: 2026-05-22

このファイルは各対象ファイルで「削除するセクション・行」と「保持するセクション」を定義する。

---

## CLAUDE.md

### 保持
- `<!-- SPECKIT START -->` マーカー行
- `<!-- SPECKIT END -->` マーカー行

### 削除
- マーカー間の全内容（plan.md参照の3行）

### 削減後の状態
```markdown
<!-- SPECKIT START -->
<!-- SPECKIT END -->
```

---

## frontend-code-rule.md（56行 → ~41行）

### 削除するセクション・内容

| 対象 | 理由 |
|---|---|
| `## 目的` セクション全体（ヘッダー + 説明文 2行） | ファイル名から目的は自明 |
| `### このプロジェクト固有の命名例（参考）` セクション全体（約10行） | 実際のコードから確認可能。ルール本体（camelCase等）は保持 |

### 保持するセクション
- `### 開発フロー`
- `### TypeScript`
- `### インポート順序`
- `### 非同期処理`
- `### 変数・関数の命名`（命名規則のルール本体のみ）
- `### その他`

---

## frontend-web-directory.md（70行 → ~48行）

### 削除するセクション・内容

| 対象 | 理由 |
|---|---|
| `## 目的` セクション全体（ヘッダー + 説明文 2行） | ファイル名から目的は自明 |
| `### このプロジェクトの実際の構造（参考）` セクション全体（約22行のファイルツリー） | ディレクトリを見れば確認可能。最も行数が多く削減効果が大きい |

### 保持するセクション
- `### ディレクトリ構成`
- `### ファイル命名規則`
- `### ゲームロジック（src/game/）`
- `### コンポーネント（src/components/）`
- `### 禁止事項`

---

## frontend-web-react-rule.md（47行 → ~39行）

### 削除するセクション・内容

| 対象 | 理由 |
|---|---|
| `## 目的` セクション全体（ヘッダー + 説明文 2行） | ファイル名から目的は自明 |
| `### このプロジェクトのコンポーネント設計パターン（参考）` セクション全体（約6行） | 実装から確認可能。ルール本体は保持 |

### 保持するセクション
- `### コンポーネント設計`
- `### Hooks`
- `### パフォーマンス`
- `### イベントハンドラ`
- `### 禁止事項`

---

## frontend-web-test-rule.md（50行 → ~42行）

### 削除するセクション・内容

| 対象 | 理由 |
|---|---|
| `## 目的` セクション全体（ヘッダー + 説明文 2行） | ファイル名から目的は自明 |
| `### このプロジェクト固有のテストパターン（参考）` セクション全体（約6行） | 実装から確認可能。ルール本体は保持 |

### 保持するセクション
- `### テストの基本方針`
- `### テストの構造`
- `### @testing-library/react の使い方`
- `### ゲームロジックのテスト`
- `### モック`
- `### 禁止事項`
